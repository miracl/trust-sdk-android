| **Functional Area**           | **MIRACLTrust**                                                                  | **MIRACLTrustCore**                                                                                    | **Difference**                       |
| ----------------------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| **Cross-Device Session**      | `getCrossDeviceSessionFromAppLink(appLink, resultHandler)`                              | `getCrossDeviceSessionFromAppLink(appLink, projectUrl, resultHandler)`                                         | Adds `projectUrl` parameter                  |
|                               | `getCrossDeviceSessionFromQRCode(qrCode, resultHandler)`                                | `getCrossDeviceSessionFromQRCode(qrCode, projectUrl, resultHandler)`                                           | Adds `projectUrl` parameter                  |
|                               | `getCrossDeviceSessionFromNotificationPayload(payload, resultHandler)`                  | `getCrossDeviceSessionFromNotificationPayload(payload, projectUrl, resultHandler)`                             | Adds `projectUrl` parameter                  |
|                               | `abortCrossDeviceSession(crossDeviceSession, resultHandler)`                            | `abortCrossDeviceSession(crossDeviceSession, projectUrl, resultHandler)`                                       | Adds `projectUrl` parameter                  |
| **Default Verification**              | `sendVerificationEmail(userId, resultHandler)`                                          | `sendVerificationEmail(userId, projectId, projectUrl, resultHandler)`                                          | Adds `projectId` and `projectUrl` parameters |
|                               | `sendVerificationEmail(userId, authenticationSessionDetails, resultHandler)`            | `sendVerificationEmail(userId, projectId, projectUrl, authenticationSessionDetails, resultHandler)`            | Adds `projectId` and `projectUrl` parameters |
|                               | `sendVerificationEmail(userId, crossDeviceSession, resultHandler)`                      | `sendVerificationEmail(userId, projectId, projectUrl, crossDeviceSession, resultHandler)`                      | Adds `projectId` and `projectUrl` parameters |
| **QuickCode**                 | `generateQuickCode(user, pinProvider, resultHandler)`                                   | `generateQuickCode(user, projectUrl, pinProvider, resultHandler)`                                              | Adds `projectUrl` parameter                  |
| **Activation & Registration** | `getActivationToken(verificationUri, resultHandler)`                                    | `getActivationToken(verificationUri, projectUrl, resultHandler)`                                               | Adds `projectUrl` parameter                  |
|                               | `getActivationToken(userId, code, resultHandler)`                                       | `getActivationToken(userId, code, projectUrl, resultHandler)`                                                  | Adds `projectUrl` parameter                  |
|                               | `register(userId, activationToken, pinProvider, pushNotificationsToken, resultHandler)` | `register(userId, projectId, projectUrl, activationToken, pinProvider, pushNotificationsToken, resultHandler)` | Adds `projectId` and `projectUrl` parameters |
| **Authentication**            | `authenticate(user, pinProvider, resultHandler)`                                        | `authenticate(user, projectUrl, pinProvider, resultHandler)`                                                   | Adds `projectUrl` parameter                  |
|                               | `authenticate(user, crossDeviceSession, pinProvider, resultHandler)`                    | `authenticate(user, crossDeviceSession, projectUrl, pinProvider, resultHandler)`                               | Adds `projectUrl` parameter                  |
|                               | `authenticateWithAppLink(user, appLink, pinProvider, resultHandler)`                    | `authenticateWithAppLink(user, appLink, projectUrl, pinProvider, resultHandler)`                               | Adds `projectUrl` parameter                  |
|                               | `authenticateWithQRCode(user, qrCode, pinProvider, resultHandler)`                      | `authenticateWithQRCode(user, qrCode, projectUrl, pinProvider, resultHandler)`                                 | Adds `projectUrl` parameter                  |
|                               | `authenticateWithNotificationPayload(payload, pinProvider, resultHandler)`              | `authenticateWithNotificationPayload(payload, projectUrl, pinProvider, resultHandler)`                         | Adds `projectUrl` parameter                  |
| **Signing**                   | `sign(message, user, pinProvider, resultHandler)`                                       | `sign(message, user, projectUrl, pinProvider, resultHandler)`                                                  | Adds `projectUrl` parameter                  |
|                               | `sign(crossDeviceSession, user, pinProvider, resultHandler)`                            | `sign(crossDeviceSession, user, projectUrl, pinProvider, resultHandler)`                                       | Adds `projectUrl` parameter                  |
| **User Management**           | `getUsers()`                                                                            | Same                                                                                                           | No change                                    |
|                               | `getUsers(resultHandler)`                                                               | Same                                                                                                           | No change                                    |
|                               | `getUser(userId)`                                                                       | `getUser(userId, projectId)`                                                                                   | Adds `projectId` parameter                   |
|                               | `getUser(userId, resultHandler)`                                                        | `getUser(userId, projectId, resultHandler)`                                                                    | Adds `projectId` parameter                   |
|                               | `delete(user)`                                                                          | Same                                                                                                           | No change                                    |
|                               | `delete(user, resultHandler)`                                                           | Same                                                                                                           | No change                                    |
